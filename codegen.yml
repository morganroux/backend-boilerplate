overwrite: true
schema: "http://localhost:3001/graphql"
generates:
  src/api/graphql/codegen/generated.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
